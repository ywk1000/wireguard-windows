logic:

- manage tunnels toolbar does not display the delete action
- Tunnel saving validation and actual saving
- Tunnel stats view in manage_tunnels
- Export tunnel + log
- Handling tunnel state updates in main window (status checkbox)
- Handling tunnel state updates in notify icon (active cidrs)
- Handling tunnel addition/deletion in TunnelsNotification
- Add button to toggle tunnel status in the ConfView

styling:

- manage_tunnels toolbar has no icons
- manage tunnels toolbar should/could be displayed like the mac gui (need a rebar)
- invalid min size for edit popup
- stats view
- tunnels list

extra:

- wordings for titles
- wordings for about box
- default template for new tunnel
- default name for new tunnel
